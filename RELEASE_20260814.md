# XMON-Grid Publication Candidate — Corrected Scientific Release Basis

## Current authoritative package

The single current scientific result authority is:

`results/authoritative_validation_20260815/`

It contains the regenerated primary detector trace, five-seed summary, case-wise and attack-wise summaries, physical-sanity data, retained publication figures, figure manifest, and SHA256 manifest.

## Validated claims

- IEEE 9-, 14-, 30-, and 118-bus benchmark definitions are checked against canonical PYPOWER/MATPOWER case data.
- Analytical Jacobian finite-difference maximum absolute discrepancy is approximately `2.7e-9`.
- Repository model and cross-case tests pass in the release pipeline.
- Independent physical AC audit passes for all four benchmark cases.
- The five-seed regenerated K=2 aggregate is F1 `0.9204 +/- 0.0026`, recall `0.8850 +/- 0.0012`, FPR `0.1525 +/- 0.0197`, and MCC `0.6667 +/- 0.0151`.
- Publication figures are generated directly from the authoritative detector trace and frozen summary CSVs; no publication metric is hard-coded in the figure generator.

## Deliberately excluded claims

The release does not claim the previously reported sub-0.6% FPR, 85.85% recall aggregate, or fixed computational speedup/power-law timing results. Those values are historical and are not authoritative for the corrected regenerated run.

The former computational-scaling publication figure is excluded from the retained publication figure set.

## Validation scope and limitations

The study uses canonical IEEE benchmark networks and controlled synthetic seeded measurement/attack data rather than field SCADA data. It therefore supports a reproducible benchmark study, not hardware, HIL, field, or operational deployment validation.

## Reproducibility

A clean checkout runs the mathematical checks, unit/cross-case tests, independent physical audit, five-seed regeneration, publication-figure regeneration, manuscript consistency patch, publication-release audit, manuscript compilation, and provenance checks. The release workflow freezes the generated authoritative artifacts back into Git so that the repository and CI state are identical.
